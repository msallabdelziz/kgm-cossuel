<form method="post" action="{{ path('app_nature_batiment_delete', {'id': nature_batiment.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir réaliser cette suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nature_batiment.id) }}">
    <button class="btn">Delete</button>
</form>
