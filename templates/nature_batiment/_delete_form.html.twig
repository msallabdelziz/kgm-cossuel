<form method="post" action="{{ path('app_nature_batiment_delete', {'id': nature_batiment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nature_batiment.id) }}">
    <button class="btn">Delete</button>
</form>
