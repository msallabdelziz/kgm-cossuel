{% extends 'base.html.twig' %}

{% block title %}TypeConstruction{% endblock %}

{% block body %}
    <h1>TypeConstruction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_construction.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ type_construction.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_construction.nom }}</td>
            </tr>
            <tr>
                <th>Usages</th>
                <td>{{ type_construction.usages }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_construction_index') }}">back to list</a>

    <a href="{{ path('app_type_construction_edit', {'id': type_construction.id}) }}">edit</a>

    {{ include('type_construction/_delete_form.html.twig') }}
{% endblock %}
