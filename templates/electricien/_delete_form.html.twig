<form method="post" action="{{ path('app_electricien_delete', {'id': electricien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ electricien.id) }}">
    <button class="btn">Delete</button>
</form>
