{% extends 'base.html.twig' %}

{% block title %}NatureTravaux{% endblock %}

{% block body %}
    <h1>NatureTravaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nature_travaux.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ nature_travaux.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ nature_travaux.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nature_travaux_index') }}">back to list</a>

    <a href="{{ path('app_nature_travaux_edit', {'id': nature_travaux.id}) }}">edit</a>

    {{ include('nature_travaux/_delete_form.html.twig') }}
{% endblock %}
